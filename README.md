# Info

Creare un virtual enviroment, attivarlo e successivamente installare le dipendenze necessarie per il progetto con il seguento comando 

```bash
pip install -r requirements.txt
```

Il codice è stato scritto con **Python 3.13.7**, al suo interno ci sono alcuni commenti riguardo al suo funzionamento.

Una volta avviato lo script, al momento traccia i pacchetti su tutte le porte di rete del computer, verranno visualizzati a terminale i pacchetti ancora da aggiungere all'interno della funzione ```def handle_packet()``` .

Avviando anche la patch max si possono vedere i pacchetti inviati tramite OSC.